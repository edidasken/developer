| Module File | Purpose (1–2 sentences) | Key Exports / Public API | Firestore Collections Touched | Events Emitted / Listened | Primary Consumers |
|---|---|---|---|---|---|
| `bezalel_codex.js` | Auto-generated build metadata file that records the line count of `B-Master Code.md` at last build time. Generated by `A-Build_Churches.sh`; do not edit manually. | None (metadata comment only) | None | None | Build tooling reference |
| `feed.js` *(app.feed/)* | Standalone bootstrap for The Feed PWA (`app.feed/`). Provides a full sermon preparation and management surface: sermon library with search and series grouping, structured outline builder (intro, scripture, points, illustrations, applications, transitions, conclusion), full manuscript editor with word count and delivery timing. Auth-gated via `Nehemiah.getSession()`. | Sermon library, outline builder, manuscript editor — all internal to the module | `sermons`, `sermonSeries` (via Firebase compat SDK) | Listens to delegated `click`, `input`, `change` | `app.feed/feed.html` |
| `flockchat.js` *(app.flockchat/)* | Standalone bootstrap for the FlockChat PWA (`app.flockchat/`). Provides church team messaging: channels, DMs, real-time message streams, and typing indicators via direct Firestore compat SDK. Auth reads `Nehemiah.getSession()` — no own login screen. Typing uses a Firestore heartbeat (TTL 5s) — no Realtime Database required. | Channel list, message thread, DM drawer, composer — all internal to the module | `conversations`, `messages`, `typing` (via Firebase compat SDK) | Listens to Firestore `onSnapshot`, delegated `click`, `keydown` | `app.flockchat/app.flockchat.html` |
| `flockshow.js` *(app.flockshow/)* | Standalone bootstrap for the FlockShow PWA (`app.flockshow/`). Provides a church presentation/projector app: show library with search, create, duplicate, delete; slide editor (lyrics, scripture, announcement, blank types); live colour + font-size controls; lyrics import (auto-split on blank lines); present mode (opens projector window, keyboard/swipe navigable). | Show library, slide editor, present mode — all internal to the module | None (all data stored in `localStorage`) | Listens to delegated `click`, `keydown`, `storage` | `app.flockshow/app.flockshow.html` |
| `grow_public.js` | Lightweight bootstrap for the public-facing GROW page (`app.grow/`). Registers a curated subset of gospel modules (no auth, no Firebase, no backend) and handles tab navigation for the public spiritual-growth surface. | `NAV` (module registry), tab navigation handlers | None | None | `app.grow/app.grow.html` |
| `the_tabernacle.js` | Legacy monolithic UI engine (`Modules` global) that provides live-data rendering for every sidebar module in the old `index.html` shell. Handles data fetching, form submissions, modals, and GAS/Firestore integration for all pre-New-Covenant views. | `render`, `applyVisibility`, `isModuleEnabled`, `_newXxx`, `_editXxx`, `_svcXxx`, `_standXxx` (many helpers) | Virtually all collections via `UpperRoom` and `TheVine` | Listens to `window.matchMedia`, `beforeunload`, delegated `click` | `index.html` (legacy shell), `TheShepherd`, `TheSeason`, `TheLife`, `TheTruth` |
| `fine_linen.js` | This module serves as the legacy theme engine, managing and persisting color palettes, dark mode, and interface overrides for the older FlockOS UI. It also provides helper functions for applying and retrieving theme names. | `themes`, `themeMeta`, `init`, `setTheme`, `getTheme`, `getGlobalTheme`, `applyOverrides`, `loadOverrides`, `clearOverrides`, `loadStudioFonts`, `OVERRIDE_LS_KEY` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) user preference APIs for theme storage. | None | `TheVine` (global), `localStorage` (for theme storage), `UpperRoom` (global) |
| `firm_foundation.js` | Responsible for all authentication and authorization within FlockOS, including user login, logout, registration, password management, and enforcing role-based access control for application features. It also offers a local security bypass for development. | `isAuthenticated`, `guard`, `guardLogin`, `getSession`, `getProfile`, `hasRole`, `hasGroup`, `canAccess`, `can`, `requireRole`, `login`, `logout`, `register`, `forgotPassword`, `resetWithCode`, `setupVault`, `loginOffline`, `hasVault`, `destroyVault`, `enableLocalBypass`, `disableLocalBypass`, `isLocalBypass` | Interacts with Firebase Authentication and `UpperRoom` (Firebase Firestore) for user data and preferences, or `TheVine` (Google Apps Script) for authentication and user management. | None | `Nehemiah` (global), `TheVine` (global), `UpperRoom` (global), `TheWellspring` (global), `firebase` (global) |
| `the_adornment.js` | This module manages the theme selection for the new FlockOS shell, applying chosen CSS variable sets to the document's `<html>` element and synchronizing with the user's system color scheme preference. | `choices`, `current`, `applyTheme`, `applyAuto`, `init` | None | Listens to `window.matchMedia('(prefers-color-scheme: dark)')` `change` events. | `the_ark.js` |
| `the_ark.js` | The main entry point for the new FlockOS shell, orchestrating the initialization of essential services like theming, error handling, authentication, routing, and data caching before rendering the initial application view. | `flock.start`, `flock.current`, `flock.theme`, `flock` (global) | Orchestrates modules that interact with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script). | Listens to `window.error`, `unhandledrejection`, `DOMContentLoaded`. | HTML entry point (via `?covenant=new` query parameter). |
| `the_cistern.js` | A lightweight IndexedDB wrapper providing persistent key-value storage for larger client-side data such as cached API responses or session-related tokens, ensuring data survives page reloads. | `read`, `write`, `remove`, `clear`, `keys` | None (uses IndexedDB) | None | `the_manna.js`, `the_scrolls/index.js`, `the_upper_room/the_identity.js`, `the_priesthood/the_breastplate.js` |
| `the_comms.js` | Provides a unified interface for all communication surfaces in FlockOS, abstracting interactions with GAS-backed ledgers (`scrolls`), Firebase-backed real-time chat (`upper`), and the embedded FlockChat PWA. | `flockchat`, `summary`, `scrolls` (re-export), `upper` (re-export) | Orchestrates modules that interact with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script). | None | Modules needing to interact with communication channels (e.g., dashboard widgets, message composers). |
| `the_fold.js` | Manages the administration of groups, small groups, Bible studies, and attendance records within the church. It offers a UI for listing, searching, editing, and managing these community structures. | `renderApp`, `switchTab`, `editGroup`, `editAttendance`, `_search` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for group and attendance management. | None | `TheLife` (global), `Modules` (global), `TheScrolls` (global), `the_ark.js` (registers it as a view). |
| `the_harvest.js` | Presents a consolidated ministry hub dashboard, allowing users to manage events, sermons, service plans, songs, ministry teams, and volunteer schedules through a single interface. | `renderHub`, `refresh`, `switchTab`, `_newEvent`, `_newSermon`, `_newServicePlan`, `_newSong`, `_newMinistry`, `_newVolunteer`, `_editEvent`, `_editSermon`, `_editServicePlan`, `_editSong`, `_editMinistry`, `_editVolunteer`, `_sermonSeries`, `_vcardOpen`, `resetHome` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for ministry-related data. | None | `TheLife` (global), `Modules` (global), `Nehemiah` (global), `TheScrolls` (global), `the_ark.js` (registers it as a view). |
| `the_lampstand.js` | Controls the application's splash screen, displaying a branded loading animation upon boot to obscure initial content loading and ensure a smooth user experience. | `kindle`, `darken`, `isLit` | None | None | `the_ark.js` |
| `the_legacy_bridge.js` | Provides an adapter layer that enables new ES modules to safely access and interact with legacy global-namespace JavaScript modules by polling the `window` object until they are available. | `bridge`, `when`, `callWhen` | None | None | All ES modules that interact with legacy globals. |
| `the_living_water_adapter.js` | Creates a flexible data access abstraction that prioritizes Firebase Firestore (via `UpperRoom`) and falls back to Google Apps Script (via `TheVine`) for various API calls, ensuring data operations are compatible across different deployment models. | `buildAdapter` | Orchestrates calls to `UpperRoom` (Firebase Firestore) methods. | None | Modules that need a unified API for both Firestore and GAS, like `the_gospel_shared.js`. |
| `the_living_water_register.js` | Manages the registration and lifecycle of the Service Worker, enabling offline functionality and automatic updates for the Progressive Web App (PWA) to ensure users always have the latest version. | `register`, `unregister` | None | Listens to `updatefound`, `statechange` (from `ServiceWorkerRegistration`), `controllerchange` (from `navigator.serviceWorker`). | `the_ark.js` |
| `the_manna.js` | Implements an in-memory caching layer with configurable time-to-live (TTL), request deduplication, and stale-while-revalidate (SWR) patterns for API responses, with optional persistence to `the_cistern.js`. | `draw`, `swr`, `invalidate`, `peek`, `write`, `hydrate`, `hydrateAll`, `wrap` | Persists/reads to/from `the_cistern.js` (IndexedDB). Wraps `UpperRoom` (Firebase Firestore) and `TheVine` (Google Apps Script) APIs. | None | `the_ark.js`, `the_true_vine.js` (implicitly via `nurture`), `the_comms.js` (indirectly via `UpperRoom`/`TheVine` wrapping). |
| `the_oil.js` | Centralizes animation primitives like fade and slide transitions, ensuring a consistent motion language across the application and respecting user preferences for reduced motion. | `fade`, `slide`, `prefersReducedMotion` | None | Listens to `window.matchMedia('(prefers-reduced-motion: reduce)')` changes. | `the_lampstand.js` |
| `the_scrolls.js` | Manages a unified interaction ledger, logging every contact (calls, texts, notes, care actions) in a searchable timeline. It stores interactions locally and syncs them to a backend for analytics. | `TYPES`, `META`, `log`, `timeline`, `feed`, `search`, `stats`, `renderApp`, `renderTimeline`, `renderPersonTimeline`, `_appSearch`, `_appFilter` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for statistics snapshots. | None | `Nehemiah` (global), `TheVine` (global), `UpperRoom` (global), `TheFold` (global), `TheShepherd` (global), `the_harvest.js`. |
| `the_shepherd.js` | Provides a comprehensive people management interface, enabling administrators to search, filter, view profiles, and perform multi-table updates across user, member, and contact card records. | `renderApp`, `openProfile`, `saveAll`, `backToList`, `_search`, `_filter`, `_approve`, `_deny`, `_createMember`, `_createCard`, `_selectPreset`, `_onAdvChkChange`, `_savePerms`, `_loadPerms`, `_loadVolunteers`, `_loadHistory`, `_deleteUser`, `_deleteMember`, `_resetPasscode`, `_createUserAccount` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for user, member, contact card, and permissions data. | None | `Modules` (global), `TheVine` (global), `UpperRoom` (global), `Nehemiah` (global), `TheScrolls` (global), `TheLife` (global), `the_ark.js` (registers it as a view). |
| `the_stones.js` | A declarative validation utility that provides a set of reusable rules for input fields, returning clear, actionable messages to guide users in correcting errors. | `rules`, `weigh`, `weighAll` | None | None | `the_priesthood/the_garments.js` |
| `the_true_vine.js` | The central API client for FlockOS, routing all backend calls to Google Apps Script (GAS) endpoints across four "Gospel-named" branches. It handles authentication, data normalization, caching, and provides a pub/sub event bus. | `app` (or `matthew`), `flock` (or `john`), `missions` (or `mark`), `extra` (or `luke`), `manifest`, `cache`, `nurture`, `grove`, `groves`, `lifecycle`, `on`, `off`, `emit`, `session`, `hasRole`, `health`, `configure`, `endpoints`, `isLocal` | None (acts as a client to GAS APIs) | Emits: `key:loading`, `key:fresh`, `key:error`, `key:cached`, `key:invalidated`, `cache:cleared`, `name:ready`, `name:refreshed`, `name:refreshed:all`, `name:teardown`, `lifecycle:paused`, `lifecycle:resumed`, `lifecycle:online`, `lifecycle:offline`, `lifecycle:started`. Listens to `visibilitychange`, `online`, `offline`, `beforeunload`. | `Nehemiah` (global), `the_manna.js`, `the_ark.js`, `the_life/index.js`, `the_pillars.js`, `the_wellspring.js`, `the_truth.js`. |
| `the_truth.js` | Provides a full CRUD interface for editing public content data (e.g., Books, Theology, Devotionals) directly within the church's Firebase Firestore project. | `render`, `openEditor`, `save`, `del`, `renderList`, `editFromTable`, `filter`, `goPage`, `switchTab`, `currentTab`, `syncBundle`, `forceSyncAll`, `liveBundle`, `bundleLastSynced` | Directly accesses Firestore collections by `tab.key` for CRUD operations on `books`, `theology`, `teachingPlans`, `genealogy`, `counseling`, `devotionals`, `reading`, `heart`, `mirror`, `quiz`, `apologetics`, `missionsRegistry`, `lexiconGreek`, `lexiconHebrew`. | None | `TheTruth` (global), `TheVine` (global), `Nehemiah` (global), `TheLife` (global), `the_ark.js` (registers it as a view), `the_gospel/the_gospel_teaching_plans.js`. |
| `the_watchmen.js` | Implements telemetry and error reporting, calmly surfacing application errors to the user via toast notifications and forwarding them to a server-side logging endpoint if configured. | `report`, `count` | Interacts with `TheVine.john.telemetry.error()` (Google Apps Script). | None | `the_ark.js` (for global error handling). |
| `the_well.js` | Facilitates database backup, restore, and template generation for FlockOS data, allowing users to create blank .xlsx schemas, download live data, and upload files for data restoration. | `template`, `templateAll`, `backup`, `backupAll`, `restore`, `schema`, `status`, `SCHEMA` | Interacts with `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) bulk/list/config APIs. | None | `TheWell` (global), `Modules` (global). |
| `the_wellspring.js` | Enables an offline-first data layer for FlockOS by allowing the application to run entirely from a single local .xlsx file stored in IndexedDB, acting as a resolver for `TheVine` API calls. | `enable`, `disable`, `isActive`, `resolve`, `load`, `exportDB`, `getTab`, `findById`, `appendRow`, `updateRow`, `deleteRow`, `status`, `clearAll`, `vault` (namespace with `setup`, `unlock`, `exists`, `destroy`) | None (uses IndexedDB) | None | `firm_foundation.js`, `TheVine` (global). |
| `the_window_bridge.js` | A self-executing script that promotes global `const` variables defined in legacy (non-module) backend scripts to properties of the `window` object, making them accessible to modern ES modules. | None | None | None | `the_legacy_bridge.js` (implicitly relies on globals being on `window`). |
| `the_witness.js` | Provides runtime self-checks and assertions for the new shell, logging a grouped report to the console (developer-only) to verify the presence and registration of key UI elements and global modules. | `check` | None | None | `the_ark.js` (conditionally, via URL parameter). |
| `the_well/index.js` | Provides an ES module interface to the legacy `TheWell` global, simplifying access to its functionalities for backup, restore, and template generation. | `ready`, `live`, `exportTemplate`, `exportBackup`, `importBackup`, `schema` | Delegates to `TheWell` (global). | None | Other new ES modules needing `TheWell` functionality. |
| `vessels/the_basin.js` | Defines a custom HTML element `<flock-input>` for a consistently styled labelled input field, incorporating an inline slot for validation error messages. | `FlockInput` (custom element registration) | None | Emits `change` custom event. | UI forms using `<flock-input>`. |
| `vessels/the_censer.js` | Defines a custom HTML element `<flock-modal>` for a lightweight, accessible modal dialog. It provides slots for title, body content, and action buttons, with attributes for control and sizing. | `FlockModal` (custom element registration), `open`, `close` | None | Emits `flock-modal:close`. Listens to `click`, `keydown`. | UI components requiring modal dialogs. |
| `vessels/the_chalice.js` | Defines a custom HTML element `<flock-card>` as a simple semantic container, providing a consistently styled raised surface with padding and rounded corners for content presentation. | `FlockCard` (custom element registration) | None | None | UI components needing card-like containers. |
| `vessels/the_cup.js` | Defines a custom HTML element `<flock-select>` to wrap a native `<select>` element, offering consistent visual styling for labels and options. | `FlockSelect` (custom element registration), `options` (setter/getter) | None | Emits `change` custom event. | UI forms using `<flock-select>`. |
| `vessels/the_mantle.js` | Defines a custom HTML element `<flock-skeleton>` for displaying animated loading skeletons, offering a shape-of-content placeholder while data is being fetched. | `FlockSkeleton` (custom element registration) | None | None | UI components requiring loading state indicators. |
| `vessels/the_menorah.js` | Defines a custom HTML element `<flock-tabs>` for declarative tabbed navigation, linking slotted tab buttons to corresponding content panels based on their order. | `FlockTabs` (custom element registration) | None | Emits `change` custom event. | UI components needing tabbed interfaces. |
| `vessels/the_rod.js` | Defines a custom HTML element `<flock-progress>` for rendering determinate or indeterminate progress bars, providing visual feedback for ongoing operations. | `FlockProgress` (custom element registration) | None | None | UI components needing progress indicators. |
| `vessels/the_seal.js` | Defines a custom HTML element `<flock-button>` for a styled button with consistent visual chrome, supporting various tones, sizes, and disabled/loading states. | `FlockButton` (custom element registration) | None | None | UI forms and components requiring interactive buttons. |
| `vessels/the_signet.js` | Defines a custom HTML element `<flock-toggle>` for an accessible boolean toggle switch, providing consistent styling and managing its checked state. | `FlockToggle` (custom element registration), `checked` (getter/setter) | None | Emits `change` custom event. | UI forms and components requiring toggle switches. |
| `vessels/the_staff.js` | Provides a global API, `raiseToast`, for displaying non-intrusive toast notifications. It is used to convey transient messages, errors, or provide undo actions to the user. | `raiseToast` | None | None | `the_watchmen.js`, other modules displaying transient messages. |
| `the_trumpet/index.js` | Provides an ES module interface to the legacy `Trumpet` global, allowing new modules to access device-specific APIs like sharing, clipboard operations, calls, SMS, and notifications. | `ready`, `live`, `share`, `clipboard`, `call`, `sms`, `notify`, `badge`, `fullscreen`, `camera`, `resizeImage`, `qr`, `geo` | Delegates to `Trumpet` (global). | None | Other new ES modules needing device API functionality. |
| `the_way/index.js` | Provides an ES module interface to the legacy `TheWay` global, facilitating access to the learning hub's features such as courses, quizzes, reading plans, and theological content. | `ready`, `live`, `listCourses`, `enrolCourse`, `submitQuiz`, `readingPlan`, `lexiconLookup`, `apologeticsList`, `counselingList`, `devotionalToday`, `certificateFor`, `analyticsFor` | Delegates to `TheWay` (global). | None | Other new ES modules needing learning hub functionality. |
| `the_shofar/index.js` | Provides an ES module interface to the legacy `shofar` global, allowing new modules to interact with the Music Stand's features including songs, arrangements, and setlists. | `ready`, `live`, `open`, `songs`, `setlists`, `stand`, `transpose`, `exportPdf` | Delegates to `shofar` (global). | None | Other new ES modules needing Music Stand functionality. |
| `the_life/index.js` | Provides an ES module interface to the legacy `TheLife` global, serving as a pastoral command hub with features for care cases, prayer requests, compassion, and outreach. It also eagerly warms up the care badge count. | `ready`, `live`, `careCases`, `prayerRequests`, `compassionList`, `pendingCount`, `subscribeOpenCareCount`, `setOpenCareCount`, `getWarmCareRows`, `subscribeOpenCareRows`, `outreachList`, `discipleshipFor`, `commsLog`, `notesFor` | Calls `UpperRoom` (Firebase Firestore) for prayers and care cases. Calls `TheVine` (Google Apps Script) for prayer, care, and compassion data. | Emits `flockos:badges:refresh` (implicitly via `window.addEventListener`). Listens to `flockos:badges:refresh`. | `the_comms.js`, `the_pillars.js`. |
| `the_scrolls/index.js` | Provides an ES module interface to the legacy `TheScrolls` global, managing an interaction ledger and offering client-side caching for member names to improve performance. | `ready`, `live`, `timeline`, `log`, `forPerson`, `types`, `filter` | Calls `TheVine` (Google Apps Script) for interactions, care, and members. Calls `UpperRoom` (Firebase Firestore) for members and care. Persists/reads member name map to/from `the_cistern.js` (IndexedDB). | None | `the_comms.js`. |
| `the_scrolls/the_bible_link.js` | Automatically converts plain text scripture references within the DOM into clickable links to bible.com (ESV), dynamically linking new content using a MutationObserver. | `bibleUrl`, `installScriptureLinks` | None | None | `the_ark.js` |
| `the_fold/index.js` | ES module wrapper over the legacy `TheFold` global, delegating all groups and attendance operations via `the_legacy_bridge.js`. | `ready`, `live` (delegates to `TheFold`) | Delegates to `TheFold` (global). | None | `the_ark.js` (registers as a view). |
| `the_shepherd/index.js` | ES module wrapper over the legacy `TheShepherd` global, delegating all people-management operations via `the_legacy_bridge.js`. | `ready`, `live` (delegates to `TheShepherd`) | Delegates to `TheShepherd` (global). | None | `the_ark.js` (registers as a view). |
| `the_truth/index.js` | ES module wrapper over the legacy `TheTruth` global, providing access to the Firestore-backed content editor for Books, Theology, Devotionals, Lexicon, and other public-content tabs. | `ready`, `live`, `render`, `openEditor`, `save`, `del`, `syncBundle`, `liveBundle` | Delegates to `TheTruth` (global). | None | `content-admin` view, `the_gospel/the_gospel_teaching_plans.js`. |
| `the_scribes/index.js` | Client-side router (public API) for the New Covenant shell. Maps URL↔view name, manages history, and surfaces the ⌘K command palette. Imports `the_path.js`, `the_chronicle.js`, and `the_herald.js`. | `register`, `go`, `current`, `replace`, `back`, `registerCommand`, `setMountSlot`, `reload` | None | Emits custom navigation events; listens to `popstate`. | `the_ark.js`, `the_veil/index.js`. |
| `the_scribes/the_path.js` | Parses URL hash fragments into view name + params objects, and builds hash strings from them. | `parse`, `build` | None | None | `the_scribes/index.js`. |
| `the_scribes/the_chronicle.js` | Manages the client-side history stack, wrapping `history.pushState` and `history.replaceState` with a typed interface. | `push`, `replace`, `current` | None | Listens to `popstate`. | `the_scribes/index.js`. |
| `the_scribes/the_herald.js` | Implements the ⌘K command palette, registering and surfacing all app-wide keyboard shortcuts and navigable commands. | `wakeHerald`, `registerCommand` | None | Listens to `keydown` (⌘K / Ctrl+K). | `the_scribes/index.js`. |
| `the_veil/index.js` | Shell chrome orchestrator for the New Covenant shell. Mounts the top bar, side nav, main content slot, footer, and pull-to-refresh handle. Runs once on boot. | `dress` | None | None | `the_ark.js`. |
| `the_veil/the_crown.js` | Renders the top bar (`#the-veil-top`), including the logo, app title, and the account/profile button wired to `the_priesthood`. | `mountCrown` | None | None | `the_veil/index.js`. |
| `the_veil/the_pillars.js` | Renders the sidebar navigation (`#the-veil-side`), populating nav entries from the route registry and surfacing care/unread badge counts. | `mountPillars` | Reads badge counts via `the_upper_room`, `the_life`, `the_seasons`. | Listens to `flockos:badges:refresh`. | `the_veil/index.js`. |
| `the_veil/the_courtyard.js` | Renders the main content slot (`#the-holy-place`), acting as the view mount point that `the_scribes` injects rendered views into. | `mountCourtyard` | None | None | `the_veil/index.js`. |
| `the_veil/the_hem.js` | Renders the footer (`#the-veil-foot`) with app version, unread indicator, and Upper Room notification status. | `mountHem` | Reads from `the_upper_room/index.js` for unread total. | None | `the_veil/index.js`. |
| `the_veil/the_refresh.js` | Attaches a native-feeling pull-to-refresh gesture to `#the-holy-place`. On release calls `the_scribes.reload()` to re-run the current view's render+mount cycle. Threshold: 72 px pull. | `mountRefresh` | None | Listens to `touchstart`, `touchmove`, `touchend`. | `the_veil/index.js`. |
| `the_priesthood/the_garments.js` | Renders the sign-in form overlay (`garments-overlay`), providing a branded login UI with email/passcode fields and inline validation via `the_stones.js`. | `renderGarments` | None | None | `the_priesthood/index.js`. |
| `the_gospel/the_gospel_courses.js` | Displays guided discipleship tracks — sermon series, theology playlists, and member-led learning paths — with filtering and enrollment status. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` for learning playlists, topics, and progress. | None | `the_ark.js` (registers as a view), `grow_public.js`. |
| `the_gospel/the_gospel_invitation.js` | Presents a hope-filled introduction to Jesus Christ through three pillars: His Great Invitations, His "I AM" Declarations, and His Finished Work. Includes a native Share button for evangelism. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (static content) | None | `the_ark.js` (registers as a view), `grow_public.js`, `app.invite/`. |
| `the_gospel/the_gospel_missions.js` | Displays a searchable world-missions view with per-nation gospel access stats, persecution data, unreached peoples counts, and daily prayer targets sourced from `Data/missions.js`. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/missions.js`). | None | `the_ark.js` (registers as a view), `grow_public.js`. |
| `the_gospel/the_gospel_psalms.js` | Browses all 150 Psalms by theme or numeric order, sourcing content from `Data/psalms.js`. Themes: Praise, Lament, Thanksgiving, Trust, Wisdom, and more. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/psalms.js`). | None | `the_ark.js` (registers as a view), `grow_public.js`. |
| `the_gospel/the_gospel_why.js` | Public GROW module that embeds `app.embeds/embed-about.html` in an iframe, surfacing the canonical "About FlockOS" content without duplicating it. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None | None | `grow_public.js`. |
| `the_upper_room/index.js` | The central import point for Firebase-backed communication features (The Upper Room), re-exporting sub-modules for identity, channels, messages, presence, typing, unread counts, mentions, emojis, seeding, attachments, and push notifications. | `firebaseConfig`, `tenant`, `identity`, `channels`, `messages`, `dms`, `presence`, `typing`, `unread`, `mentions`, `emoji`, `seeding`, `attachments`, `push`, `churchId`, `unreadTotal` | Orchestrates modules that directly interact with Firebase Firestore/Realtime Database. | None (re-exports modules that handle events) | `the_ark.js`, `the_comms.js`, `the_pillars.js`, `the_life/index.js` |
| `the_upper_room/the_attachments.js` | Manages file and image uploads to Firebase Storage for chat messages, including validation for file size and returning stable URLs for uploaded content. | `upload`, `validate` | Delegates to `TheUpperRoom.uploadAttachment()` (Firebase Storage). | None | Chat message composers. |
| `the_upper_room/the_channels.js` | Provides an ES module interface for managing chat channels (listing, watching, creating, updating, archiving), acting as a facade over the legacy `TheUpperRoom` global's conversation methods. | `ready`, `list`, `watch`, `create`, `update`, `archive` | Delegates to `TheUpperRoom` (Firebase Firestore) conversation methods. | Exposes `onChange` callback for `watch`. | Chat UI components that display/manage channels, `the_upper_room/the_seeding.js`. |
| `the_upper_room/the_dms.js` | Manages direct message (DM) threads, providing functions to generate deterministic thread IDs, list threads, retrieve messages, and send new messages. | `ready`, `threadIdFor`, `list`, `messages`, `send`, `openWith`, `watch` | Delegates to `TheUpperRoom` (Firebase Firestore) DM and message methods. | Exposes `onChange` callback for `watch`. | Chat UI components for direct messages. |
| `the_upper_room/the_emoji.js` | Offers a small, curated set of emojis and a lightweight emoji picker UI for quick selection, suitable for chat applications. | `categories`, `symbolsFor`, `all`, `pick` | None | None | Chat message composers or other UI components needing emoji selection. |
| `the_upper_room/the_firebase_config.js` | Resolves the Firebase web configuration for the current church, prioritizing a build-injected config and falling back to a shared project if no specific church configuration is available. | `getConfig`, `getVapidKey`, `getProjectId`, `hasOwnProject` | None | None | `the_upper_room/the_identity.js`, `the_upper_room/the_push.js`. |
| `the_upper_room/the_identity.js` | Bridges the authentication between the Nehemiah session and Firebase, minting custom Firebase tokens via a Cloud Function and caching them for single sign-on to Firebase services. | `mint`, `signInToFirebase`, `forget` | Stores tokens in `the_cistern.js` (IndexedDB). Calls a Cloud Function (`mintFlockchatToken`). | None | Firebase-backed modules within `the_upper_room/` that require user authentication. |
| `the_upper_room/the_mentions.js` | Provides utility functions to parse `@mentions` from text and render them with highlighting, enabling social features in chat and comments. | `parse`, `render` | None | None | Chat message display components. |
| `the_upper_room/the_messages.js` | Provides an ES module interface for managing message streams (listing, watching, sending, deleting) within a channel or DM thread, acting as a facade over the legacy `TheUpperRoom` global's message methods. | `ready`, `watch`, `list`, `send`, `remove`, `edit`, `react` | Delegates to `TheUpperRoom` (Firebase Firestore) message methods. | Exposes `onChange` callback for `watch`. | Chat UI components that display/send messages. |
| `the_upper_room/the_presence.js` | Manages user online/away/offline presence status in real-time using Firebase Realtime Database's `onDisconnect` functionality. | `ready`, `setMine`, `watch`, `read` | Delegates to `TheUpperRoom` (Firebase Realtime Database) presence methods. | Exposes `onChange` callback for `watch`. | Chat UI components that display user presence. |
| `the_upper_room/the_push.js` | Handles Firebase Cloud Messaging (FCM) token registration and unregistration for web push notifications, allowing devices to receive push messages. | `ready`, `register`, `unregister`, `enabled` | Delegates to `TheUpperRoom` (Firebase Firestore) push notification methods. | None | UI components allowing users to manage push notification preferences. |
| `the_upper_room/the_seeding.js` | Ensures a set of default chat channels (e.g., 'general', 'announcements', 'prayer-chain') are present in a church's Firebase project, creating them if they don't exist. | `SEEDS`, `ensureSeeds`, `seed` | Delegates to `TheUpperRoom` (Firebase Firestore) channel creation/browsing methods. | None | Chat module initialization logic. |
| `the_upper_room/the_tenant.js` | Resolves the current `churchId` and constructs Firebase Firestore and Realtime Database paths, ensuring all chat data operations are correctly scoped to the active church. | `churchId`, `fsPath`, `rtdbPath`, `channelsCol`, `dmsCol`, `presenceRtdb` | None | None | Other modules within `the_upper_room/` for pathing and church ID. |
| `the_upper_room/the_typing.js` | Manages and displays typing indicators in chat channels, throttling updates to Firebase Realtime Database to optimize performance. | `ping`, `watch` | Delegates to `TheUpperRoom` (Firebase Realtime Database) typing methods. | Exposes `onChange` callback for `watch`. | Chat message input composers and chat display components. |
| `the_upper_room/the_unread.js` | Tracks unread message counts for chat channels by persisting "last read" timestamps in `localStorage`, which is used to calculate badge totals across sessions. | `mark`, `lastRead`, `count`, `totalFor` | None (uses `localStorage`) | None | Chat channel list components (for badges), `the_upper_room/index.js` (for `unreadTotal`). |
| `the_gospel/the_gospel_analytics.js` | Renders a comprehensive dashboard for discipleship analytics, showcasing individual learning progress through stats, activity heatmaps, quiz results, and earned certificates. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` (Firebase Firestore) learning-related dashboard, progress, quiz, and certificate APIs. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_apologetics.js` | Presents a categorized collection of apologetics questions and biblical answers, providing scriptural and reasoned responses to common objections to the Christian faith. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/apologetics.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_certificates.js` | Displays a list of a user's earned course completion certificates, offering options to view and print these acknowledgements of their discipleship journey. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` (Firebase Firestore) learning certificates API. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_counseling.js` | Presents biblical counseling topics, offering definitions, relevant scriptures, and practical steps for addressing various life trials like anxiety, grief, and addiction. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/counseling.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_devotionals.js` | Displays daily devotionals, each including a scripture passage, a reflection, and a prayer, aiming to provide spiritual nourishment and a quiet time before daily activities. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/devotionals.js`). | Listens to `click` events for toggling `<details>`. | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_genealogy.js` | Presents a searchable record of biblical figures and families, detailing name meanings, lifespans, biographies, and familial connections within the redemption story. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/genealogy.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_heart.js` | Provides an interactive "Heart Check" spiritual diagnostic quiz, guiding users through Yes/No questions to identify areas for personal reflection and offering scripturally-rooted prescriptions. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for app content data. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_journal.js` | Offers a private digital journal for users to record their spiritual insights, prayers, and reflections, providing CRUD capabilities for their entries. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` (Firebase Firestore) for journal entries. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_lexicon.js` | Provides a searchable lexicon for Greek and Hebrew word studies, detailing word meanings, transliterations, Strong's numbers, and verse appearances. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/strongs-greek.js` and `../../Data/strongs-hebrew.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_library.js` | Offers a guided tour of the 66 books of the Bible, providing summaries, core theological insights, and practical applications for each book to aid in understanding. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/library.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_mirror.js` | Implements a "Shepherd's Mirror" soul triage diagnostic, presenting categorized questions for self-assessment and generating a strategic action plan with scriptural prescriptions. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `UpperRoom` (Firebase Firestore) or `TheVine` (Google Apps Script) for app content data. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_quizzes.js` | Provides Bible challenges, course assessments, and self-check quizzes, allowing users to test their knowledge, track their scores, and review areas for spiritual growth. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/quiz.js`). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_reading.js` | Manages daily Bible reading plans, including a full one-year plan, and visualizes personal reading streaks through an activity heatmap. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/one_year_bible.js`). Uses `localStorage` for reading progress. | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_shared.js` | A collection of reusable utility functions and helpers (e.g., HTML escaping, date formatting, loading states) shared across all modules within the "The Gospel" (learning hub) section. | `ur`, `vine`, `session`, `rows`, `esc`, `mdInline`, `stripMd`, `stripHtml`, `snip`, `fmtDate`, `timeAgo`, `emptyState`, `backendOffline`, `loadingCards`, `bibleLink`, `chip`, `sectionHead`, `askForHelp`, `helpButton`, `wireHelp` | Calls `UpperRoom` (Firebase Firestore) for prayer requests. Calls `TheVine` (Google Apps Script) for session data. | None | All modules in `New_Covenant/Scripts/the_gospel/`. |
| `the_gospel/the_gospel_teaching_plans.js` | Displays pre-built teaching plans, outlining sessions and segments for discipleship and teaching moments, sourcing content from either a live Firestore bundle or a static fallback. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | Calls `TheTruth.liveBundle('teachingPlans')` (which accesses Firebase Firestore). | None | `the_ark.js` (registers it as a view). |
| `the_gospel/the_gospel_theology.js` | Presents a structured map of biblical doctrine, allowing users to explore theological categories, their associated sections, supporting scriptures, and practical applications. | `name`, `title`, `description`, `icon`, `accent`, `render`, `mount` | None (loads static data from `../../Data/theology.js`). | None | `the_ark.js` (registers it as a view). |
| `the_harvest/index.js` | Provides an ES module interface to the legacy `TheHarvest` global, allowing new modules to access ministry hub features such as events, sermons, service plans, teams, and volunteers. | `ready`, `live`, `listEvents`, `saveEvent`, `listSermons`, `saveSermon`, `listServicePlans`, `saveServicePlan`, `listTeams`, `listVolunteers` | Delegates to `TheHarvest` (global). | None | Other new ES modules needing ministry hub functionality. |
| `the_seasons/index.js` | Provides an ES module interface to the legacy `TheSeason` global, facilitating access to calendar, tasks, check-in, and iCal feed functionalities. It also offers a specific count for today's events. | `ready`, `live`, `todayCount`, `calendar`, `tasks`, `checkIn`, `icalFeed` | Calls `TheVine.flock.events.list()` (Google Apps Script) for today's events. Delegates to `TheSeason` (global). | None | `the_pillars.js`. |
| `the_wellspring/index.js` | Provides an ES module interface to the legacy `TheWellspring` global, allowing new modules to manage local data mode, including enabling/disabling, loading/exporting data, and vault management. | `ready`, `live`, `isActive`, `load`, `exportXlsx`, `status` | Delegates to `TheWellspring` (global). | None | Other new ES modules needing local data mode functionality. |
| `the_priesthood/index.js` | Provides the authentication UI shell, acting as an ES module wrapper over the `Nehemiah` global. It handles user sign-in/out flows and opens the account settings drawer. | `whoAmI`, `profile`, `enter`, `depart`, `openAccountSheet`, `readToken` | Delegates to `Nehemiah` (global) for core authentication. | None | `the_ark.js`, `the_veil/the_crown.js`. |
| `the_priesthood/the_anointing.js` | Provides a utility function to securely read the authentication token from `localStorage`, ensuring identity for subsequent API calls. | `readToken` | None (reads `localStorage`). | None | `the_upper_room/the_identity.js`, `the_priesthood/index.js`. |
| `the_priesthood/the_breastplate.js` | Renders a slide-out drawer for user account settings, offering tabs for profile details, notification preferences, and personal iCal/member card links. | `renderBadge` | Calls `UpperRoom` (Firebase Firestore) for notification preferences, calendar share tokens, and member card search. | None | `the_priesthood/index.js`. |
| `the_priesthood/the_garments.js` | Renders the sign-in form user interface, managing form submission, client-side validation, and interaction with the `Nehemiah` authentication module. | `renderGarments` | Calls `Nehemiah.login()` (Google Apps Script). | None | `the_priesthood/index.js`. |
| `the_shepherd/index.js` | Provides an ES module interface to the legacy `TheShepherd` global, facilitating access to people management features such as member search, profile viewing, and multi-table data saving. | `ready`, `live`, `search`, `getProfile`, `save`, `create`, `can` | Delegates to `TheShepherd` (global). | None | Other new ES modules needing people management functionality. |
| `the_scribes/index.js` | The central client-side router, responsible for mapping URLs to views, managing browser history, and providing a command palette for quick navigation and action execution. | `setMountSlot`, `register`, `go`, `current`, `reload`, `historyCurrent` | None | Listens to `window.popstate`. | `the_ark.js`, `the_veil/the_pillars.js`, `the_veil/the_crown.js`, `the_scribes/the_herald.js`. |
| `the_scribes/the_chronicle.js` | A wrapper around the browser's `history` API, providing simplified functions to push and replace history states for client-side routing within the application. | `push`, `replace`, `current` | None | None | `the_scribes/index.js`. |
| `the_scribes/the_herald.js` | Implements a global command palette, accessible via ⌘K, allowing users to quickly search for and execute actions or navigate to different parts of the application. | `wakeHerald`, `registerCommand`, `toggle`, `open`, `close` | None | Listens to `keydown` (⌘K, Escape), `click`. | `the_scribes/index.js`, `the_veil/the_crown.js`. |
| `the_scribes/the_path.js` | Provides utility functions to build and parse URLs, mapping application view names and their parameters to browser history paths while preserving essential query parameters. | `build`, `parse` | None | None | `the_scribes/index.js`. |
| `the_fold/index.js` | Provides an ES module interface to the legacy `TheFold` global, facilitating access to community management features such as groups, small groups, and attendance. | `ready`, `live`, `listGroups`, `getGroup`, `saveGroup`, `recordAttendance`, `attendanceFor` | Delegates to `TheFold` (global). | None | Other new ES modules needing group/attendance functionality. |
| `the_veil/index.js` | The primary module for constructing the overall application shell, including the top bar, side navigation, main content slot, and footer, then delegating to sub-modules for their specific UI elements. | `dress` | None | Listens to `click`. | `the_ark.js`. |
| `the_veil/the_courtyard.js` | Mounts and configures the main content area (the "courtyard") within the application's shell, providing a consistent, scrollable container for dynamically loaded views. | `mountCourtyard` | None | None | `the_veil/index.js`. |
| `the_veil/the_crown.js` | Mounts and manages the top navigation bar of the application shell, providing access to branding, search, notifications, and user account actions. | `mountCrown` | None | Listens to `click`. | `the_veil/index.js`. |
| `the_veil/the_hem.js` | Mounts and populates the application's footer, displaying copyright information, dynamic church ID, and navigation links. | `mountHem` | None | None | `the_veil/index.js`. |
| `the_veil/the_pillars.js` | Mounts and manages the side navigation bar, displaying categorized menu items with icons and dynamic badges, and handling navigation to different views. | `mountPillars` | Calls `UpperRoom` (Firebase Firestore) for unread message counts and `TheLife` (Firebase Firestore/Google Apps Script) for pending care counts. | Listens to `click`, `window.popstate`, `window.flockos:badges:refresh`. | `the_veil/index.js`. |
| `the_veil/the_refresh.js` | Implements a pull-to-refresh gesture on the main scrollable content area, triggering a reload of the current view when the user pulls down from the top. | `mountRefresh` | None | Listens to `touchstart`, `touchmove`, `touchend`. | `the_veil/index.js`. |

---

### Dependency Graph

*   `the_ark.js` → imports from: `the_lampstand.js`, `the_veil/index.js`, `the_scribes/index.js`, `the_priesthood/index.js`, `the_watchmen.js`, `the_adornment.js`, `the_living_water_register.js`, `the_scrolls/the_bible_link.js`
*   `the_comms.js` → imports from: `the_scrolls/index.js`, `the_upper_room/index.js`, `the_life/index.js`
*   `the_lampstand.js` → imports from: `the_oil.js`
*   `the_manna.js` → imports from: `the_cistern.js`
*   `the_well/index.js` → imports from: `the_legacy_bridge.js`
*   `vessels/the_staff.js` → No imports.
*   `the_trumpet/index.js` → imports from: `the_legacy_bridge.js`
*   `the_way/index.js` → imports from: `the_legacy_bridge.js`
*   `the_shofar/index.js` → imports from: `the_legacy_bridge.js`
*   `the_life/index.js` → imports from: `the_legacy_bridge.js`
*   `the_scrolls/index.js` → imports from: `the_legacy_bridge.js`, `the_cistern.js`
*   `the_upper_room/index.js` → imports from: `the_legacy_bridge.js`, `the_upper_room/the_firebase_config.js`, `the_upper_room/the_tenant.js`, `the_upper_room/the_identity.js`, `the_upper_room/the_channels.js`, `the_upper_room/the_messages.js`, `the_upper_room/the_dms.js`, `the_upper_room/the_presence.js`, `the_upper_room/the_typing.js`, `the_upper_room/the_unread.js`, `the_upper_room/the_mentions.js`, `the_upper_room/the_emoji.js`, `the_upper_room/the_seeding.js`, `the_upper_room/the_attachments.js`, `the_upper_room/the_push.js`
*   `the_upper_room/the_attachments.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_channels.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_dms.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_identity.js` → imports from: `the_cistern.js`, `the_priesthood/the_anointing.js`, `the_upper_room/the_tenant.js`, `the_upper_room/the_firebase_config.js`
*   `the_upper_room/the_messages.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_presence.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_push.js` → imports from: `the_legacy_bridge.js`, `the_upper_room/the_firebase_config.js`
*   `the_upper_room/the_seeding.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_typing.js` → imports from: `the_legacy_bridge.js`
*   `the_upper_room/the_unread.js` → imports from: `the_upper_room/the_tenant.js`
*   `the_gospel/the_gospel_analytics.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_apologetics.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_certificates.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_counseling.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_devotionals.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_genealogy.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_heart.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_journal.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_lexicon.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_library.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_mirror.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_quizzes.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_reading.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_teaching_plans.js` → imports from: `the_gospel_shared.js`
*   `the_gospel/the_gospel_theology.js` → imports from: `the_gospel_shared.js`
*   `the_harvest/index.js` → imports from: `the_legacy_bridge.js`
*   `the_seasons/index.js` → imports from: `the_legacy_bridge.js`
*   `the_wellspring/index.js` → imports from: `the_legacy_bridge.js`
*   `the_priesthood/index.js` → imports from: `the_priesthood/the_garments.js`, `the_priesthood/the_anointing.js`, `the_priesthood/the_breastplate.js`
*   `the_priesthood/the_breastplate.js` → No imports.
*   `the_priesthood/the_garments.js` → imports from: `the_stones.js`
*   `the_shepherd/index.js` → imports from: `the_legacy_bridge.js`
*   `the_scribes/index.js` → imports from: `the_scribes/the_path.js`, `the_scribes/the_chronicle.js`, `the_scribes/the_herald.js`
*   `the_scribes/the_herald.js` → No imports.
*   `the_scribes/the_path.js` → No imports.
*   `the_fold/index.js` → imports from: `the_legacy_bridge.js`
*   `the_veil/index.js` → imports from: `the_veil/the_crown.js`, `the_veil/the_pillars.js`, `the_veil/the_courtyard.js`, `the_veil/the_hem.js`, `the_scribes/index.js`, `the_veil/the_refresh.js`
*   `the_veil/the_courtyard.js` → No imports.
*   `the_veil/the_crown.js` → imports from: `the_priesthood/index.js`
*   `the_veil/the_hem.js` → imports from: `the_upper_room/index.js`
*   `the_veil/the_pillars.js` → imports from: `the_scribes/index.js`, `the_upper_room/index.js`, `the_life/index.js`
*   `the_veil/the_refresh.js` → No imports.
| Module File | Purpose (1–2 sentences) | Key Exports / Public API | Firestore Collections Touched | Events Emitted / Listened | Primary Consumers |
|---|---|---|---|---|---|
| `New_Covenant/Scripts/the_life.js` | Provides a comprehensive pastoral command-hub, enabling management of spiritual care cases, prayer requests, compassion requests, and outreach contacts through dedicated full-page editors. It also includes functionality for member profile management and permission assignment. | `renderHub`, `openCareCase`, `saveCareCase`, `openPrayer`, `savePrayer`, `openCompassion`, `saveCompassion`, `openOutreach`, `saveOutreach`, `openAddMember`, `saveMember`, `savePermissions`, `loadMemberCareHistory`, `_callPhone`, `_smsPhone` | `careCases`, `careInteractions`, `prayerRequests`, `compassionRequests`, `compassionLogs`, `outreachContacts`, `outreachFollowUps`, `members`, `appConfig`, `careAssignments`, `permissions`, `auditLog`, `statisticsSnapshots` | None explicit, but calls `Modules._toast`, `Modules._modal`, `Modules._ppOpen`, `Modules._undoAction`, `Modules.render`, `Modules.onboardMember`, `Modules._printReport` | `the_tabernacle.js` |
| `New_Covenant/Scripts/the_way.js` | Offers a unified learning and growth portal, facilitating access to structured courses, interactive quizzes, reading plans, theological content, a Greek/Hebrew lexicon, devotionals, apologetics, and tracking user progress and achievements. | `renderHub`, `switchTab`, `openCourse`, `_markComplete`, `_saveNote`, `_newCourse`, `_editCourse`, `_startAppQuiz`, `_scoreQuiz`, `_startCourseQuiz`, `_openReadingPlan`, `_toggleReadingDay`, `_openCounselCard`, `_twLexSelect`, `_twGeneSelect`, `_heartAnswer`, `_newJournal`, `_viewJournal`, `_printCert` | `learningPlaylists`, `learningPlaylistItems`, `learningTopics`, `learningProgress`, `learningNotes`, `learningCertificates`, `learningQuizzes`, `learningQuizResults`, `theologyCategories`, `theologySections`, `sermons`, `journal`, `appContent` | `firebase.auth().onAuthStateChanged` (via `UpperRoom.init`) | `the_tabernacle.js` |
| `New_Covenant/Scripts/the_upper_room.js` | Serves as the dedicated Firebase Firestore communication module, providing a robust API for all data interactions across various church management domains, including real-time listeners and authentication via custom tokens. | `init`, `authenticate`, `signOut`, `isReady`, `createDM`, `sendMessage`, `listPrayers`, `createTodo`, `listMembers`, `createCareCase`, `listLrnPlaylists`, `listTheologyCategories`, `listMissionsRegistry`, `getAppConfig`, `getUserPreferences`, `listAudit`, `deleteUserCascade` | `conversations`, `templates`, `broadcasts`, `settings`, `prayers`, `todos`, `notifications`, `users` (deprecated), `members`, `memberCards`, `permissions`, `memberRoles`, `careCases`, `careInteractions`, `careAssignments`, `compassionRequests`, `compassionLogs`, `compassionResources`, `outreachContacts`, `outreachCampaigns`, `outreachFollowUps`, `events`, `rsvps`, `calendarEvents`, `calendarTokens`, `groups`, `attendance`, `checkinSessions`, `volunteers`, `ministries`, `servicePlans`, `songs`, `songArrangements`, `sermons`, `sermonSeries`, `sermonReviews`, `giving`, `pledges`, `journal`, `discipleshipPaths`, `discipleshipSteps`, `discipleshipEnrollments`, `discipleshipMentoring`, `discipleshipMeetings`, `discipleshipGoals`, `discipleshipAssessments`, `discipleshipMilestones`, `discipleshipCertificates`, `learningTopics`, `learningPlaylists`, `learningPlaylistItems`, `learningQuizzes`, `learningQuizResults`, `learningRecommendations`, `learningProgress`, `learningNotes`, `learningCertificates`, `theologyCategories`, `theologySections`, `albums`, `statisticsConfig`, `statisticsSnapshots`, `statisticsViews`, `missionsRegistry`, `missionsPartners`, `missionsPrayerFocus`, `missionsUpdates`, `missionsTeams`, `appConfig`, `preferences`, `contactLog`, `pastoralNotes`, `milestones`, `households`, `auditLog`, `accessControl` | `firebase.auth().onIdTokenChanged`, `firebase.auth().onAuthStateChanged`, `firestore.onSnapshot` (various collections) | `the_tabernacle.js`, `the_vine.js`, `the_life.js`, `the_way.js`, `the_seasons.js`, and other frontend modules |
| `New_Covenant/Scripts/the_seasons.js` | Implements a comprehensive calendar, task, and check-in hub, integrating church-wide events, personal schedules, external iCal feeds, task management for users, and attendance recording functionality. | `renderCalendar`, `renderTodo`, `renderCheckin`, `calNav`, `calToday`, `calSetView`, `calQuickAdd`, `calEventDetail`, `calRefresh`, `calSwitchMode`, `calSubscribe`, `calDownloadICS`, `calDelegation`, `calNewPersonal`, `calEditPersonal`, `calDeletePersonal`, `calEditEvent`, `calCancelEvent`, `rsvpToEvent`, `viewEventRsvps`, `newTask`, `editTask`, `completeTask`, `openCheckinSession` | `events`, `servicePlans`, `calendarEvents`, `calendarTokens`, `todos`, `careInteractions`, `appConfig`, `rsvps`, `checkinSessions`, `members` | None explicit, but calls `Modules._toast`, `Modules._modal`, `Modules._edit`, `Modules._reload`, `Modules._table`, `Modules._statusBadge`, `Modules._trumpetShare`, `Modules._svcOpen`, `Modules.geoCheckin`, `Modules.toggleFullscreen` | `the_tabernacle.js` |

---
### Dependency Graph

*   `the_life.js` → imports from: `the_tabernacle.js`, `the_true_vine.js`, `firm_foundation.js`, `fine_linen.js`, `the_upper_room.js`
*   `the_way.js` → imports from: `the_tabernacle.js`, `the_true_vine.js`, `firm_foundation.js`, `fine_linen.js`, `the_upper_room.js`, `../Data/quiz.js`, `../Data/reading-plans.js`, `../Data/theology.js`, `../Data/apologetics.js`, `../Data/devotionals.js`, `../Data/psalms.js`
*   `the_upper_room.js` → imports from: `firebase`, `the_true_vine.js`, `firm_foundation.js`
*   `the_seasons.js` → imports from: `the_tabernacle.js`, `the_true_vine.js`, `firm_foundation.js`, `the_upper_room.js`
| Module File | Purpose (1–2 sentences) | Key Exports / Public API | Firestore Collections Touched | Events Emitted / Listened | Primary Consumers |
|---|---|---|---|---|---|
| New_Covenant/Scripts/the_tabernacle.js | Provides the core UI rendering logic and data orchestration for most application modules (e.g., Directory, Events, Settings). It handles data fetching, form submissions, UI updates, and integrates various backend services while adapting behavior based on the active backend (Firebase or Google Apps Script). | `render(name, el, session)`: Main function to render a specified UI module. `applyVisibility()`: Applies module visibility rules. `isModuleEnabled(name)`: Checks if a module is enabled. Numerous helper functions for UI, data manipulation, modals, and specific module actions (`newXxx`, `editXxx`, `viewXxx`, `_pgXxx`, `_adXxx`, `_svcXxx`, `_standXxx`, etc.). | Directly accesses `presence`, `scheduledReports`, `masterConfig`, `problems`, and `churches/{churchId}/settings/sync`. Via the `UpperRoom` abstraction layer, it interacts with most application data collections including `appConfig`, `members`, `memberCards`, `groups`, `attendance`, `events`, `sermons`, `albums`, `servicePlans`, `songs`, `ministries`, `volunteers`, `compassionRequests`, `discipleship`, `outreachContacts`, `learningPlaylists`, `theologyCategories`, `journal`, `prayers`, `conversations`, `notifications`, `missionsRegistry`, `giving`, `statsSnapshots`, and `contacts`. | **Emitted:** None explicitly.<br>**Listened:** `window.matchMedia('...').addEventListener('change', ...)`, `window.addEventListener('beforeunload', ...)`, `document.addEventListener('click', ...)` (delegated for editable table rows). | `index.html` (for main module rendering and navigation), global `navigate()` function, other modules (`the_vine.js` for `_perfProfile`), and various delegated handlers (`TheShepherd.renderApp`, `TheSeason.renderTodo`, etc.) that are orchestrated by `Modules`. Inline `onclick` attributes within rendered HTML also directly consume many exposed functions. |

### Dependency Graph
(Based on `import` statements and explicit global variable usage within `the_tabernacle.js`.)

the_tabernacle.js → relies on globals:
  - TheVine
  - UpperRoom
  - Nehemiah
  - Adornment
  - TheSeason
  - TheLife
  - TheShepherd
  - TheTruth
  - Trumpet
  - firebase
  - session
  - navigate
  - _DEFAULT_APP_CONFIG
  - refreshNotifBadge
  - openPrayerRequestApp
